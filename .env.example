APP_HOST=localhost
APP_PORT=8080

# DB
DB_HOST=localhost
DB_PORT=5432
DB_USER=
DB_PASS=
DB_NAME=
DB_URL=postgresql://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=disable

# REDIS
RDB_HOST=localhost
RDB_PORT=6379
RDB_USER=
RDB_PASS=
RDB_PREFIX=
RDB_ADDR=${RDB_HOST}:${RDB_PORT}

# SMTP
SMTP_HOST=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=care@shortlink.dev

# Reset Password URL
RESET_PASSWORD_URL=http://localhost:5173/reset-password?token=

JWT_ISSUER=
JWT_SECRET=